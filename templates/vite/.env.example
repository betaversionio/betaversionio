VITE_BETAVERSION_API_URL=http://localhost:4000/v1

# For local development without the proxy, set the username to render.
# In production the proxy injects <meta name="portfolio-username"> into index.html.
VITE_PORTFOLIO_USERNAME=satyam
